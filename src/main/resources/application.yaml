spring:
  datasource:
    username: root
    password: maksi
    url: jdbc:postgresql://localhost:5437/lectors
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8130

logging:
  level:
    org.flywaydb: DEBUG